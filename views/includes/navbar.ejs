<nav class="navbar navbar-expand-md" style="z-index: 1050;">
  <div class="container-fluid px-3 px-md-4">

    <!-- Brand -->
    <a class="navbar-brand" href="/">
      <i class="fa-solid fa-graduation-cap"></i>
    </a>

    <!-- Mobile Toggle -->
    <button class="navbar-toggler border-0" type="button"
      data-bs-toggle="collapse" data-bs-target="#navbarMain"
      aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation"
      style="box-shadow:none;">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible -->
    <div class="collapse navbar-collapse" id="navbarMain">

      <!-- Left links -->
      <div class="navbar-nav me-auto gap-1 mt-2 mt-md-0">
        <a class="nav-link" href="/">
          <i class="fa-solid fa-house me-1" style="font-size:0.8rem;"></i>Home
        </a>
        <a class="nav-link" href="/listings">
          <i class="fa-solid fa-users me-1" style="font-size:0.8rem;"></i>Educators
        </a>
        <% if (!hasListing) { %>
          <a class="nav-link" href="/listings/new">Become an Educator</a>
        <% } else { %>
          <a class="nav-link" href="/listings/<%= userListingId %>">My Educator Profile</a>
        <% } %>
      </div>

      <!-- Right actions -->
      <div class="d-flex flex-column flex-md-row align-items-stretch align-items-md-center gap-2 mt-3 mt-md-0">
        <% if (!currUser) { %>
          <a href="/signup" class="btn btn-light">Sign Up</a>
          <a href="/login"  class="btn btn-primary">Log In</a>
        <% } else { %>
          <% if (currUser && hasStudentProfile) { %>
            <a class="btn btn-light" href="/student-profile">
              <i class="fa-solid fa-circle-user me-1"></i>My Profile
            </a>
          <% } else { %>
            <a class="btn btn-light" href="/student-profile/new">Join as Student</a>
          <% } %>
          <a href="/wallet" class="btn btn-light">
            <i class="fa-solid fa-wallet me-1"></i>Wallet
          </a>
          <a href="/logout" class="btn btn-primary">Log Out</a>
        <% } %>
      </div>

    </div>
  </div>
</nav>
